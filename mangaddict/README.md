# Mangaddict 🌸

Bienvenue sur **Mangaddict**, la boutique de mangas la plus kawaii du net !

## Installation

1. **Base de données** : 
   - Ouvrez PHPMyAdmin ou votre terminal MySQL.
   - Importez le fichier `database.sql` situé à la racine.
   - Vérifiez que le fichier `config.ini` contient vos identifiants (par défaut: root / sans mot de passe).

2. **Lancement** :
   - Si vous utilisez PHP en local, placez-vous dans le dossier du projet via le terminal.
   - Lancez la commande : `php -S localhost:8000 -t public`
   - Ouvrez votre navigateur sur `http://localhost:8000`.

## Identifiants de test

Vous pouvez créer un compte via la page "Inscription" (les mots de passe sont hashés).
